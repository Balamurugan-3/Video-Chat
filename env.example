# 1. URL of your app (http://localhost:3000 for local, or https://your-site.com)
NEXTAUTH_URL=http://localhost:3000

# 2. A random password to protect sessions (type anything long)
NEXTAUTH_SECRET=my-super-secret-password-123

# 3. Google Login Keys (Get these from Google Cloud Console)
# Go to: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# 4. Redis URL (Optional - only for scaling to 1000+ users)
# REDIS_URL=redis://localhost:6379

# 5. MongoDB URI (Optional - for storing user data)
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/video-chat
